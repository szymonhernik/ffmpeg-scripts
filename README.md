# ffmpeg-scripts

## To run this bash script via terminal, follow these steps:
First, make sure the script is executable by running this command in the terminal:
   
`chmod +x process_video.sh`

Then you can run the script using either of these commands:
```
./process_video.sh
# or
bash process_video.sh
```
